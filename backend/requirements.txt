# ─────────────────────────────────────────────────────────────────────────────
# AIuthor Backend — Python Dependencies
# Pinned for reproducibility. Update intentionally.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.47.0

# Data validation & settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Environment
python-dotenv==1.0.1

# HTTP client (used by tests via httpx TestClient)
httpx==0.27.0

# Testing
pytest==8.2.0
pytest-cov==5.0.0
pytest-asyncio==0.23.6

# Admin interface
sqladmin==0.16.1

# LLM providers (Module 5.0)
google-genai==0.8.0
openai==1.30.3

# Vector storage (Module 6.0A)
pgvector==0.3.2

# Workflow orchestration (Module 7.1A)
langgraph==1.2.1

# Document generation (Module 10.0)
python-docx==1.1.2

